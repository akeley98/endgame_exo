diff --git a/src/exo/platforms/Sm90/Sm90_fwd.py b/src/exo/platforms/Sm90/Sm90_fwd.py
index 07efc648..b2cc19ab 100644
--- a/src/exo/platforms/Sm90/Sm90_fwd.py
+++ b/src/exo/platforms/Sm90/Sm90_fwd.py
@@ -18,7 +18,5 @@ from exo.spork.timelines import (
     wgmma_async_rmem_d_qual,
     cuda_rmem_qual_tl_dict,
     cuda_ram_qual_tl_dict,
-    wgmma_zero_instr,
-    wgmma_zero_qual,
 )
 from exo.spork.cuda_memory import Sm90_TmaCommitGroup, Sm90_WgmmaCommitGroup
diff --git a/src/exo/platforms/Sm90/Sm90_tk_mma.py b/src/exo/platforms/Sm90/Sm90_tk_mma.py
index d6d14ca4..6d87de4a 100644
--- a/src/exo/platforms/Sm90/Sm90_tk_mma.py
+++ b/src/exo/platforms/Sm90/Sm90_tk_mma.py
@@ -17,51 +17,219 @@ from .Sm90_tk_mma_impl import Sm90_TkRmemTileA, Sm90_TkRmemTileD, make_basic_mma
 
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+__all__ = [
+    'CudaTkWarpTile',
+    'Sm90_TkRmemTileA',
+    'Sm90_TkRmemTileD',
+    'Sm90_tk_mm_row_col',
+    'Sm90_tk_mm_rmem_col',
+    'Sm90_tk_mm_col_col',
+    'Sm90_tk_mm_row_row',
+    'Sm90_tk_mm_rmem_row',
+    'Sm90_tk_mm_col_row',
+    'Sm90_tk_mma_row_col',
+    'Sm90_tk_mma_rmem_col',
+    'Sm90_tk_mma_col_col',
+    'Sm90_tk_mma_row_row',
+    'Sm90_tk_mma_rmem_row',
+    'Sm90_tk_mma_col_row'
+]
 
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 1/12
 @instr
-class Sm90_tk_zero_scale_d(InstrInfo):
-    """Triggers the tile to be zeroed in the next mma instruction.
+class Sm90_tk_mm_row_col(make_basic_mma('row', 'col', True)):
+    """"m(64) x n(N) x k(K) matrix multiply (D = AB)
+
+    A in row-major SMEM, B in column-major SMEM"""
+    def behavior(
+        N: size,
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, N],
+        # A @ Sm90_SmemSwizzled(swizzle), indexed as [m, k]
+        A: [R][64, K],
+        # B @ Sm90_SmemSwizzled(swizzle), indexed as [n, k]
+        B: [R][N, K],
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for n in seq(0, N):
+                    D[m_warp, mi, n] = 0.0
+                    for k in seq(0, K):
+                        D[m_warp, mi, n] += (
+                            A[m_warp * 16 + mi, k]
+                          * B[n, k]
+                        )
 
-    """
-    valid_num_types = {(f16, ), (f32, )}
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 2/12
+@instr
+class Sm90_tk_mm_rmem_col(make_basic_mma('rmem', 'col', True)):
+    """"m(64) x n(N) x k(K) matrix multiply (D = AB)
 
-    def behavior(N: size, D: [R][4, 16, N]):
+    A in register tiles, B in column-major SMEM"""
+    def behavior(
+        N: size,
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, N],
+        # A @ Sm90_TkRmemTileA(K), split on M dim into 4 warps
+        A: [R][4, 16, K],
+        # B @ Sm90_SmemSwizzled(swizzle), indexed as [n, k]
+        B: [R][N, K],
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
         for m_warp in seq(0, 4):
             for mi in seq(0, 16):
                 for n in seq(0, N):
-                    D[m_warp, mi, n] = 0
+                    D[m_warp, mi, n] = 0.0
+                    for k in seq(0, K):
+                        D[m_warp, mi, n] += (
+                            A[m_warp, mi, k]
+                          * B[n, k]
+                        )
 
-    def instance(self, N):
-        self.instr_tl = wgmma_zero_instr
-        self.coll_unit = cuda_warpgroup
-        self.access_info["D"].mem = Sm90_TkRmemTileD(N)
-        self.access_info["D"].out_of_order = False
-        self.access_info["D"].distributed_coll_units = (cuda_warp, )
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 3/12
+@instr
+class Sm90_tk_mm_col_col(make_basic_mma('col', 'col', True)):
+    """"m(64) x n(N) x k(K) matrix multiply (D = AB)
 
-    def codegen(self, args):
-        return [f"{args.D.index(get_scale_d=True)} = 0;"]
+    A in column-major SMEM (trans-a), B in column-major SMEM"""
+    def behavior(
+        N: size,
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, N],
+        # A @ Sm90_SmemSwizzled(128), indexed as [k, m]
+        A: [R][K, 64] @ Sm90_SmemSwizzled(128),
+        # B @ Sm90_SmemSwizzled(swizzle), indexed as [n, k]
+        B: [R][N, K],
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for n in seq(0, N):
+                    D[m_warp, mi, n] = 0.0
+                    for k in seq(0, K):
+                        D[m_warp, mi, n] += (
+                            A[k, m_warp * 16 + mi]
+                          * B[n, k]
+                        )
 
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 4/12
+@instr
+class Sm90_tk_mm_row_row(make_basic_mma('row', 'row', True)):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply (D = AB)
+
+    A in row-major SMEM, B in row-major SMEM (trans-b)"""
+    def behavior(
+        N64: size,  # N / 64
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, 64 * N64],
+        # A @ Sm90_SmemSwizzled(swizzle), indexed as [m, k]
+        A: [R][64, K],
+        # B @ Sm90_SmemSwizzled(128), indexed as [n // 64, k, n % 64]
+        B: [R][N64, K, 64] @ Sm90_SmemSwizzled(128),
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for no in seq(0, N64):
+                    for ni in seq(0, 64):
+                        D[m_warp, mi, no * 64 + ni] = 0.0
+                        for k in seq(0, K):
+                            D[m_warp, mi, no * 64 + ni] += (
+                                A[m_warp * 16 + mi, k]
+                              * B[no, k, ni]
+                            )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-__all__ = [
-    'CudaTkWarpTile',
-    'Sm90_TkRmemTileA',
-    'Sm90_TkRmemTileD',
-    'Sm90_tk_zero_scale_d',
-    'Sm90_tk_mma_row_col',
-    'Sm90_tk_mma_rmem_col',
-    'Sm90_tk_mma_col_col',
-    'Sm90_tk_mma_row_row',
-    'Sm90_tk_mma_rmem_row',
-    'Sm90_tk_mma_col_row'
-]
+# wgmma.mma_async instr variant 5/12
+@instr
+class Sm90_tk_mm_rmem_row(make_basic_mma('rmem', 'row', True)):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply (D = AB)
 
+    A in register tiles, B in row-major SMEM (trans-b)"""
+    def behavior(
+        N64: size,  # N / 64
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, 64 * N64],
+        # A @ Sm90_TkRmemTileA(K), split on M dim into 4 warps
+        A: [R][4, 16, K],
+        # B @ Sm90_SmemSwizzled(128), indexed as [n // 64, k, n % 64]
+        B: [R][N64, K, 64] @ Sm90_SmemSwizzled(128),
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for no in seq(0, N64):
+                    for ni in seq(0, 64):
+                        D[m_warp, mi, no * 64 + ni] = 0.0
+                        for k in seq(0, K):
+                            D[m_warp, mi, no * 64 + ni] += (
+                                A[m_warp, mi, k]
+                              * B[no, k, ni]
+                            )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 6/12
+@instr
+class Sm90_tk_mm_col_row(make_basic_mma('col', 'row', True)):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply (D = AB)
+
+    A in column-major SMEM (trans-a), B in row-major SMEM (trans-b)"""
+    def behavior(
+        N64: size,  # N / 64
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, 64 * N64],
+        # A @ Sm90_SmemSwizzled(128), indexed as [k, m]
+        A: [R][K, 64] @ Sm90_SmemSwizzled(128),
+        # B @ Sm90_SmemSwizzled(128), indexed as [n // 64, k, n % 64]
+        B: [R][N64, K, 64] @ Sm90_SmemSwizzled(128),
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for no in seq(0, N64):
+                    for ni in seq(0, 64):
+                        D[m_warp, mi, no * 64 + ni] = 0.0
+                        for k in seq(0, K):
+                            D[m_warp, mi, no * 64 + ni] += (
+                                A[k, m_warp * 16 + mi]
+                              * B[no, k, ni]
+                            )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 1/6
+# wgmma.mma_async instr variant 7/12
 @instr
-class Sm90_tk_mma_row_col(make_basic_mma('row', 'col')):
-    """"m(64) x n(N) x k(K) matrix multiply-accumulate
+class Sm90_tk_mma_row_col(make_basic_mma('row', 'col', False)):
+    """"m(64) x n(N) x k(K) matrix multiply-accumulate (D += AB)
 
     A in row-major SMEM, B in column-major SMEM"""
     def behavior(
@@ -88,10 +256,10 @@ class Sm90_tk_mma_row_col(make_basic_mma('row', 'col')):
                         )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 2/6
+# wgmma.mma_async instr variant 8/12
 @instr
-class Sm90_tk_mma_rmem_col(make_basic_mma('rmem', 'col')):
-    """"m(64) x n(N) x k(K) matrix multiply-accumulate
+class Sm90_tk_mma_rmem_col(make_basic_mma('rmem', 'col', False)):
+    """"m(64) x n(N) x k(K) matrix multiply-accumulate (D += AB)
 
     A in register tiles, B in column-major SMEM"""
     def behavior(
@@ -118,10 +286,10 @@ class Sm90_tk_mma_rmem_col(make_basic_mma('rmem', 'col')):
                         )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 3/6
+# wgmma.mma_async instr variant 9/12
 @instr
-class Sm90_tk_mma_col_col(make_basic_mma('col', 'col')):
-    """"m(64) x n(N) x k(K) matrix multiply-accumulate
+class Sm90_tk_mma_col_col(make_basic_mma('col', 'col', False)):
+    """"m(64) x n(N) x k(K) matrix multiply-accumulate (D += AB)
 
     A in column-major SMEM (trans-a), B in column-major SMEM"""
     def behavior(
@@ -148,10 +316,10 @@ class Sm90_tk_mma_col_col(make_basic_mma('col', 'col')):
                         )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 4/6
+# wgmma.mma_async instr variant 10/12
 @instr
-class Sm90_tk_mma_row_row(make_basic_mma('row', 'row')):
-    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
+class Sm90_tk_mma_row_row(make_basic_mma('row', 'row', False)):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate (D += AB)
 
     A in row-major SMEM, B in row-major SMEM (trans-b)"""
     def behavior(
@@ -179,10 +347,10 @@ class Sm90_tk_mma_row_row(make_basic_mma('row', 'row')):
                             )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 5/6
+# wgmma.mma_async instr variant 11/12
 @instr
-class Sm90_tk_mma_rmem_row(make_basic_mma('rmem', 'row')):
-    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
+class Sm90_tk_mma_rmem_row(make_basic_mma('rmem', 'row', False)):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate (D += AB)
 
     A in register tiles, B in row-major SMEM (trans-b)"""
     def behavior(
@@ -210,10 +378,10 @@ class Sm90_tk_mma_rmem_row(make_basic_mma('rmem', 'row')):
                             )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 6/6
+# wgmma.mma_async instr variant 12/12
 @instr
-class Sm90_tk_mma_col_row(make_basic_mma('col', 'row')):
-    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
+class Sm90_tk_mma_col_row(make_basic_mma('col', 'row', False)):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate (D += AB)
 
     A in column-major SMEM (trans-a), B in row-major SMEM (trans-b)"""
     def behavior(
diff --git a/src/exo/platforms/Sm90/Sm90_tk_mma_impl.py b/src/exo/platforms/Sm90/Sm90_tk_mma_impl.py
index 0f8ea6e7..4f8fcc23 100644
--- a/src/exo/platforms/Sm90/Sm90_tk_mma_impl.py
+++ b/src/exo/platforms/Sm90/Sm90_tk_mma_impl.py
@@ -27,13 +27,14 @@ def Sm90_TkRmemTileD(N: int):
 
     class Tile(CudaTkWarpTile(16, N, "row")):
         qual_tl_dict = cuda_rmem_qual_tl_dict | {
-            wgmma_async_instr: [wgmma_async_rmem_d_qual, wgmma_zero_qual],
+            wgmma_async_instr: wgmma_async_rmem_d_qual,
         }
 
     return Tile
 
 
-def make_basic_mma(a_mode, b_mode):
+def make_basic_mma(a_mode, b_mode, zero_init: bool):
+    """zero_init: D = AB (scale-d = 0; "mm") vs D += AB (scale-d = 1; "mma")"""
     assert a_mode in ("row", "col", "rmem")
     assert b_mode in ("row", "col")
 
@@ -175,11 +176,11 @@ def make_basic_mma(a_mode, b_mode):
                 if K_iters != 1:
                     lines.append(f"  // K = {k * K_native} out of {self.K}")
                 d_arg = args.D.index()
-                scale_d_arg = args.D.index(get_scale_d=True)
                 ptx = InlinePtxGen(instr_fmt, volatile=True)
 
-                # p=scale_d arg to wgmma
-                ptx.add_arg(scale_d_arg, constraint="r", log_as=None, N=1)
+                # p=scale_d arg to wgmma (hardwired; true constant)
+                scale_d = 0 if (zero_init and k == 0) else 1
+                ptx.add_arg(scale_d, constraint="n", log_as=None, N=1)
 
                 # Add vector of registers (D argument)
                 if self.d_type == f32:
@@ -229,9 +230,7 @@ def make_basic_mma(a_mode, b_mode):
                 ptx.add_arg("exo_descB", constraint="l", log_as=None)
 
                 # Write out the code for this K iteration.
-                # Implicitly reset scale_d flag after wgmma.
                 lines.extend(ptx.as_c_lines(tab="  "))
-                lines.append(f"  {scale_d_arg} = 1;")
                 lines.append("}")
             # End for k in range(K_iters)
             return lines
diff --git a/src/exo/platforms/Sm90/gen_Sm90_tk_mma.py b/src/exo/platforms/Sm90/gen_Sm90_tk_mma.py
index 3329463f..0fdcb8e5 100644
--- a/src/exo/platforms/Sm90/gen_Sm90_tk_mma.py
+++ b/src/exo/platforms/Sm90/gen_Sm90_tk_mma.py
@@ -41,38 +41,13 @@ from .Sm90_tk_mma_impl import Sm90_TkRmemTileA, Sm90_TkRmemTileD, make_basic_mma
 
 
 
-_all = ["CudaTkWarpTile", "Sm90_TkRmemTileA", "Sm90_TkRmemTileD", "Sm90_tk_zero_scale_d"]
+_all = ["CudaTkWarpTile", "Sm90_TkRmemTileA", "Sm90_TkRmemTileD"]
 _instr_defs = []
 
 
-write_chunk('''
-@instr
-class Sm90_tk_zero_scale_d(InstrInfo):
-    """Triggers the tile to be zeroed in the next mma instruction.
-
-    """
-    valid_num_types = {(f16, ), (f32, )}
-
-    def behavior(N: size, D: [R][4, 16, N]):
-        for m_warp in seq(0, 4):
-            for mi in seq(0, 16):
-                for n in seq(0, N):
-                    D[m_warp, mi, n] = 0
-
-    def instance(self, N):
-        self.instr_tl = wgmma_zero_instr
-        self.coll_unit = cuda_warpgroup
-        self.access_info["D"].mem = Sm90_TkRmemTileD(N)
-        self.access_info["D"].out_of_order = False
-        self.access_info["D"].distributed_coll_units = (cuda_warp, )
-
-    def codegen(self, args):
-        return [f"{args.D.index(get_scale_d=True)} = 0;"]
-''')
-
-
-def append_instr(a_mode: str, b_mode: str):
-    instr_name = f"Sm90_tk_mma_{a_mode}_{b_mode}"
+def append_instr(a_mode: str, b_mode: str, zero_init: bool):
+    # ThunderKittens-style naming: mm (D = AB, scale-d = 0), mma (D += AB, scale-d = 1)
+    instr_name = f"Sm90_tk_{'mm' if zero_init else 'mma'}_{a_mode}_{b_mode}"
 
     M = "64"
     if a_mode == "rmem":
@@ -90,8 +65,9 @@ def append_instr(a_mode: str, b_mode: str):
 
     lines = []
     lines.append("@instr")
-    lines.append(f"class {instr_name}(make_basic_mma({a_mode!r}, {b_mode!r})):")
-    lines.append(f'    """"m({M}) x n({N}) x k(K) matrix multiply-accumulate')
+    lines.append(f"class {instr_name}(make_basic_mma({a_mode!r}, {b_mode!r}, {zero_init!r})):")
+    what = "matrix multiply (D = AB)" if zero_init else "matrix multiply-accumulate (D += AB)"
+    lines.append(f'    """"m({M}) x n({N}) x k(K) {what}')
     lines.append("")
     lines.append(f'    A in {A_comment}, B in {B_comment}"""')
     lines.append("    def behavior(")
@@ -154,6 +130,8 @@ def append_instr(a_mode: str, b_mode: str):
         lines.append(indent + "for n in seq(0, N):")
         indent += "    "
         n = "n"
+    if zero_init:
+        lines.append(indent + f"D[m_warp, mi, {n}] = 0.0")
     lines.append(indent + "for k in seq(0, K):")
     indent += "    "
 
@@ -177,9 +155,10 @@ def append_instr(a_mode: str, b_mode: str):
     _instr_defs.append("\n".join(lines))
 
 
-for b_mode in ("col", "row"):
-    for a_mode in ("row", "rmem", "col"):
-        append_instr(a_mode, b_mode)
+for zero_init in (True, False):
+    for b_mode in ("col", "row"):
+        for a_mode in ("row", "rmem", "col"):
+            append_instr(a_mode, b_mode, zero_init)
 
 
 write_chunk(
diff --git a/src/exo/platforms/Sm90/tk_gemm_util.py b/src/exo/platforms/Sm90/tk_gemm_util.py
index f2c590fb..271a51c8 100644
--- a/src/exo/platforms/Sm90/tk_gemm_util.py
+++ b/src/exo/platforms/Sm90/tk_gemm_util.py
@@ -224,6 +224,18 @@ def sched_inline_stuff(p):
     return p
 
 
+import os
+# Research flag: fold `if iter_k == 0` mm/mma selection after the cut (default on).
+WGMMA_MM_FOLD = os.environ.get("WGMMA_MM_FOLD", "1") != "0"
+
+
+def sched_fold_iter_k_0(p):
+    """After cutting iter_k at 1, statically resolve `if iter_k == 0` in both halves."""
+    for c in p.find("if iter_k == 0: _", many=True):
+        p = eliminate_dead_code(p, c)
+    return p
+
+
 def sched_cut_sync_iter_k(p, config: GemmConfig):
     """Cut the iter_k loop at 1, and add synchronization in the iter_k >= 1 loop.
 
@@ -312,6 +324,9 @@ def sched_cut_sync_iter_k(p, config: GemmConfig):
     # Only consumer executes this.
     p = wrap_with_context(p, cta_m_c, CudaWarps(name="consumer"))
 
+    if WGMMA_MM_FOLD:
+        p = sched_fold_iter_k_0(p)
+
     # (Ignore) For testing, if bug enabled, delete an Await in the iter_k >= 1 loop.
     if config.bug == GemmTestBug.missing_await_raw:
         loop_c = p.forward(loop_c)
@@ -447,14 +462,24 @@ def handwrite_row_col_coop_main_loop(config: GemmConfig):
                         for wg_m in cuda_threads(0, 2, unit=cuda_warpgroup):
                             Fence(wgmma_fence_1, wgmma_fence_2)
                             for ms in seq(0, M_wg_tiles, pragma_unroll=0):
-                                Sm90_tk_mma_row_col(
-                                    D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
-                                    A_smem[cta_m, cta_n, iter_k % ring_depth,
-                                           (wg_m * M_wg_tiles + ms) * 64 :
-                                           (wg_m * M_wg_tiles + ms) * 64 + 64, :],
-                                    B_smem[cta_m, cta_n, iter_k % ring_depth, :, :],
-                                    D=D_type, A=A_type, B=B_type, N=cta_N, K=smem_K,
-                                )
+                                if iter_k == 0:
+                                    Sm90_tk_mm_row_col(
+                                        D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
+                                        A_smem[cta_m, cta_n, iter_k % ring_depth,
+                                               (wg_m * M_wg_tiles + ms) * 64 :
+                                               (wg_m * M_wg_tiles + ms) * 64 + 64, :],
+                                        B_smem[cta_m, cta_n, iter_k % ring_depth, :, :],
+                                        D=D_type, A=A_type, B=B_type, N=cta_N, K=smem_K,
+                                    )
+                                else:
+                                    Sm90_tk_mma_row_col(
+                                        D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
+                                        A_smem[cta_m, cta_n, iter_k % ring_depth,
+                                               (wg_m * M_wg_tiles + ms) * 64 :
+                                               (wg_m * M_wg_tiles + ms) * 64 + 64, :],
+                                        B_smem[cta_m, cta_n, iter_k % ring_depth, :, :],
+                                        D=D_type, A=A_type, B=B_type, N=cta_N, K=smem_K,
+                                    )
                             Arrive(wgmma_async) >> wgmma_cg[cta_m, cta_n, wg_m]
                         # This is where it gets annoying.
                         # Each iteration except iter_k = 0 has to Await for its commit group
@@ -627,26 +652,44 @@ def handwrite_row_row_coop_main_loop(config: GemmConfig):
                                         )
                                 Fence(wgmma_fence_1, wgmma_fence_2)
                                 for ms in seq(0, M_wg_tiles, pragma_unroll=0):
-                                    Sm90_tk_mma_rmem_row(
-                                        D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
-                                        A_rmem[:, ms, :, :],
-                                        B_smem[cta_m, cta_n, iter_k % ring_depth, :, :, :],
-                                        D=D_type, A=A_type, B=B_type, N64=cta_N // 64, K=smem_K,
-                                    )
+                                    if iter_k == 0:
+                                        Sm90_tk_mm_rmem_row(
+                                            D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
+                                            A_rmem[:, ms, :, :],
+                                            B_smem[cta_m, cta_n, iter_k % ring_depth, :, :, :],
+                                            D=D_type, A=A_type, B=B_type, N64=cta_N // 64, K=smem_K,
+                                        )
+                                    else:
+                                        Sm90_tk_mma_rmem_row(
+                                            D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
+                                            A_rmem[:, ms, :, :],
+                                            B_smem[cta_m, cta_n, iter_k % ring_depth, :, :, :],
+                                            D=D_type, A=A_type, B=B_type, N64=cta_N // 64, K=smem_K,
+                                        )
                                 Arrive(wgmma_async) >> wgmma_cg[cta_m, cta_n, wg_m]
                                 Await(wgmma_cg[cta_m, cta_n, wg_m], cuda_in_order, 0)
                             else:
                                 # Normal path. Load A and B from SMEM.
                                 Fence(wgmma_fence_1, wgmma_fence_2)
                                 for ms in seq(0, M_wg_tiles, pragma_unroll=0):
-                                    Sm90_tk_mma_row_row(
-                                        D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
-                                        A_smem[cta_m, cta_n, iter_k % ring_depth,
-                                               (wg_m * M_wg_tiles + ms) * 64 :
-                                               (wg_m * M_wg_tiles + ms) * 64 + 64, :],
-                                        B_smem[cta_m, cta_n, iter_k % ring_depth, :, :, :],
-                                        D=D_type, A=A_type, B=B_type, N64=cta_N // 64, K=smem_K,
-                                    )
+                                    if iter_k == 0:
+                                        Sm90_tk_mm_row_row(
+                                            D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
+                                            A_smem[cta_m, cta_n, iter_k % ring_depth,
+                                                   (wg_m * M_wg_tiles + ms) * 64 :
+                                                   (wg_m * M_wg_tiles + ms) * 64 + 64, :],
+                                            B_smem[cta_m, cta_n, iter_k % ring_depth, :, :, :],
+                                            D=D_type, A=A_type, B=B_type, N64=cta_N // 64, K=smem_K,
+                                        )
+                                    else:
+                                        Sm90_tk_mma_row_row(
+                                            D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
+                                            A_smem[cta_m, cta_n, iter_k % ring_depth,
+                                                   (wg_m * M_wg_tiles + ms) * 64 :
+                                                   (wg_m * M_wg_tiles + ms) * 64 + 64, :],
+                                            B_smem[cta_m, cta_n, iter_k % ring_depth, :, :, :],
+                                            D=D_type, A=A_type, B=B_type, N64=cta_N // 64, K=smem_K,
+                                        )
                                 Arrive(wgmma_async) >> wgmma_cg[cta_m, cta_n, wg_m]
                         # This is where it gets annoying.
                         # Each iteration except iter_k = 0 has to Await for its commit group
@@ -791,14 +834,24 @@ def handwrite_row_col_ping_pong_main_loop(config: GemmConfig):
                             # Each warpgroup does its own WGMMAs, then arrives on its own commit group.
                             Fence(wgmma_fence_1, wgmma_fence_2)
                             for ms in seq(0, M_wg_tiles, pragma_unroll=0):
-                                Sm90_tk_mma_row_col(
-                                    D_rmem[cta_m, cta_n, ping, :, ms, :, :],
-                                    A_smem[cta_m, cta_n, ping, iter_k,
-                                           ms * 64 :
-                                           ms * 64 + 64, :],
-                                    B_smem[cta_m, cta_n, ping, iter_k, :, :],
-                                    D=D_type, A=A_type, B=B_type, N=cta_N, K=smem_K,
-                                )
+                                if iter_k == 0:
+                                    Sm90_tk_mm_row_col(
+                                        D_rmem[cta_m, cta_n, ping, :, ms, :, :],
+                                        A_smem[cta_m, cta_n, ping, iter_k,
+                                               ms * 64 :
+                                               ms * 64 + 64, :],
+                                        B_smem[cta_m, cta_n, ping, iter_k, :, :],
+                                        D=D_type, A=A_type, B=B_type, N=cta_N, K=smem_K,
+                                    )
+                                else:
+                                    Sm90_tk_mma_row_col(
+                                        D_rmem[cta_m, cta_n, ping, :, ms, :, :],
+                                        A_smem[cta_m, cta_n, ping, iter_k,
+                                               ms * 64 :
+                                               ms * 64 + 64, :],
+                                        B_smem[cta_m, cta_n, ping, iter_k, :, :],
+                                        D=D_type, A=A_type, B=B_type, N=cta_N, K=smem_K,
+                                    )
                             Arrive(wgmma_async) >> wgmma_cg[cta_m, cta_n, ping]
                         # This is where it gets annoying.
                         # Each iteration except iter_k = 0 has to Await for its commit group
@@ -1165,15 +1218,6 @@ def handwrite_row_col_gemm(config: GemmConfig):
                             #
                             # m = wg_m * (M_wg_tiles * 64) + ms * 64 + w * 16 + mt
                             D_rmem: D_type[ncta_M, ncta_N, 2, 4, M_wg_tiles, 16, cta_N] @ D_tile_mem
-                            for cta_m in cuda_threads(0, ncta_M, unit=ncta_N * cuda_cta_in_cluster):
-                                for cta_n in cuda_threads(0, ncta_N, unit=cuda_cta_in_cluster):
-                                    with CudaWarps(name="consumer"):
-                                        for wg_m in cuda_threads(0, 2, unit=cuda_warpgroup):
-                                            for ms in seq(0, M_wg_tiles, pragma_unroll=0):
-                                                Sm90_tk_zero_scale_d(
-                                                    D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
-                                                    D=D_type, N=cta_N,
-                                                )
 
                             main_loop(
                                 K_cluster,
@@ -1311,15 +1355,6 @@ def handwrite_row_row_gemm(config: GemmConfig):
                             #
                             # m = wg_m * (M_wg_tiles * 64) + ms * 64 + w * 16 + mt
                             D_rmem: D_type[ncta_M, ncta_N, 2, 4, M_wg_tiles, 16, cta_N] @ D_tile_mem
-                            for cta_m in cuda_threads(0, ncta_M, unit=ncta_N * cuda_cta_in_cluster):
-                                for cta_n in cuda_threads(0, ncta_N, unit=cuda_cta_in_cluster):
-                                    with CudaWarps(name="consumer"):
-                                        for wg_m in cuda_threads(0, 2, unit=cuda_warpgroup):
-                                            for ms in seq(0, M_wg_tiles, pragma_unroll=0):
-                                                Sm90_tk_zero_scale_d(
-                                                    D_rmem[cta_m, cta_n, wg_m, :, ms, :, :],
-                                                    D=D_type, N=cta_N,
-                                                )
 
                             main_loop(
                                 K_cluster,
@@ -1579,6 +1614,15 @@ def schedule_gemm(config: GemmConfig, cases=None):
     gemm = divide_loop(gemm, k_loop, smem_K, ("iter_k", "sub_iter_k"), tail="guard")
     iter_k_loop = gemm.forward(k_loop)
     sub_iter_k_loop = iter_k_loop.only_child()
+    # D_rmem = 0; for iter_k: ...
+    # -> for iter_k: (if iter_k == 0: D_rmem = 0); ...
+    # Needs K_cluster > 0 (checked by add_loop).
+    D_zero = gemm.forward(D_zero)
+    gemm = add_loop(gemm, D_zero, "iter_k", f"(K_cluster + {smem_K - 1}) / {smem_K}", guard=True)
+    iter_k_loop = gemm.forward(iter_k_loop)
+    gemm = fuse(gemm, iter_k_loop.prev(), iter_k_loop)
+    iter_k_loop = gemm.forward(iter_k_loop.prev())  # fuse deletes 2nd loop
+    gap_after_main = iter_k_loop.after()
     k_lifts = 0
     parent = iter_k_loop.parent()
     while True:
@@ -1588,11 +1632,9 @@ def schedule_gemm(config: GemmConfig, cases=None):
         k_lifts += 1
         parent = parent.parent()
     gemm = lift_alloc(gemm, D_rmem, n_lifts=k_lifts)
-    gemm = fission(gemm, gap_before_main, n_lifts=k_lifts, unsafe_disable_checks=unsafe)
     gemm = fission(gemm, gap_after_main, n_lifts=k_lifts, unsafe_disable_checks=unsafe)
     for i in range(k_lifts):
         gemm = lift_scope(gemm, iter_k_loop)
-    gap_before_main = gemm.forward(gap_before_main)
     gap_after_main = gemm.forward(gap_after_main)
     D_rmem = gemm.forward(D_rmem)
     iter_k_loop = gemm.forward(iter_k_loop)
@@ -1726,7 +1768,6 @@ def schedule_gemm(config: GemmConfig, cases=None):
     wgmma_mw_cursor = wgmma_ms_cursor.only_child(1)
     assert wgmma_mw_cursor.name() == "mw"
     gemm = unsafe_remove_if(gemm, wgmma_ms_cursor, True)
-    gemm = replace(gemm, wgmma_mw_cursor, Sm90_tk_mma_row_col)
     gemm = insert_fence(gemm, wgmma_ms_cursor.before(), wgmma_fence_1, wgmma_fence_2)
     # Arrive(wgmma_async, 1) >> wgmma_cg[cta_m, cta_n, wg_m]
     gemm = insert_arrive(gemm, wgmma_ms_cursor.after(), wgmma_async, "wgmma_cg[cta_m, cta_n, wg_m]")
@@ -1739,15 +1780,6 @@ def schedule_gemm(config: GemmConfig, cases=None):
     gemm = wrap_with_context(gemm, iter_k_loop.body()[:2], CudaWarps(0, 1, name="producer"))
     gemm = wrap_with_context(gemm, iter_k_loop.body()[2], CudaWarps(name="consumer"))
 
-    # Finalize zero prologue.
-    # We have to unsafely remove the if-guards to substitute the zero instr.
-    D_zero = gemm.forward(D_zero)
-    zero_m_loop = find_parent_loop(gemm, D_zero, "wg_m")
-    gemm = unsafe_remove_if(gemm, zero_m_loop, True)
-    gemm = wrap_with_context(gemm, zero_m_loop, CudaWarps(name="consumer"))
-    zero_m_loop = gemm.forward(zero_m_loop)
-    gemm = replace(gemm, zero_m_loop.find("for mw in _:_"), Sm90_tk_zero_scale_d)
-
     # Finalize write-to-C epilogue.
     # Need to wait for wgmma beforehand, and do a final arrive to war barrier.
     # This wait loop is fissioned out from the epilogue.
@@ -1907,6 +1939,15 @@ def schedule_gemm(config: GemmConfig, cases=None):
     if True:
         gemm = sched_cut_sync_iter_k(gemm, config)
 
+    # Substitute wgmma now that iter_k == 0 is peeled (and folded):
+    # mm (scale-d = 0) for the first iteration, mma (scale-d = 1) for the rest.
+    assert WGMMA_MM_FOLD, "schedule_gemm requires WGMMA_MM_FOLD"
+    for mw_loop in gemm.find("for mw in _:_", many=True):
+        mw_loop = gemm.forward(mw_loop)
+        iter_k_lo = find_parent_loop(gemm, mw_loop, "iter_k")._impl._node.lo
+        is_first = getattr(iter_k_lo, "val", None) == 0
+        gemm = replace(gemm, mw_loop, Sm90_tk_mm_row_col if is_first else Sm90_tk_mma_row_col)
+
     gemm = simplify(gemm)
     proc_name = config.make_proc_name(True)
     gemm = rename(gemm, proc_name)
diff --git a/src/exo/platforms/kittens_impl/tk_types.py b/src/exo/platforms/kittens_impl/tk_types.py
index bb6afb02..21661c60 100644
--- a/src/exo/platforms/kittens_impl/tk_types.py
+++ b/src/exo/platforms/kittens_impl/tk_types.py
@@ -60,33 +60,13 @@ __all__.append("cuda_tk_vec_layout_names")
 __all__.append("cuda_tk_valid_num_types_all_pairs")
 
 
-_cuda_tk_scale_d_code = """\
-#ifdef __cplusplus
-template <typename Tile>
-struct exo_CudaTkScaleD: public Tile
-{
-    // Set to 0 to trigger a zero-clear on the next async mma instr.
-    // Each async mma instr resets this to 1.
-    int scale_d = 1;
-    Tile tile;
-};
-#endif
-"""
-
-_cuda_tk_scale_d_global = MemGlobalC("exo_CudaTkScaleD", _cuda_tk_scale_d_code, [])
-
-
 class _CudaTkWarpTileIndexer(WindowIndexer):
-    def index(self, utils: UtilInjector, features: WindowFeatures, get_scale_d=False):
+    def index(self, utils: UtilInjector, features: WindowFeatures):
         utils.add_cu_include("kittens.cuh")
         c_expr = features.get_dataptr()
         # All non-packed indices resolve to multidimensional array indexing.
         for i in range(features.n_array_dims()):
             c_expr = c_expr[features.get_array_offset(i)]
-        if get_scale_d:
-            c_expr = c_expr.scale_d  # Get scale_d attribute
-        else:
-            c_expr = c_expr.tile
         return self.pack_result(c_expr, False)
 
 
@@ -119,8 +99,7 @@ def CudaTkWarpTile(r, c, layout="row"):
     (f32 is only supported for D; tf32 operand support is omitted).
 
     If implementing an instruction, use arg.index() to get a reference
-    to the tile, and arg.index(get_scale_d=True) to get a reference
-    to the magic scale_d member.
+    to the tile.
 
     """
     assert r % 16 == 0, f"CudaTkWarpTile requires r={r} to be divisible by 16"
@@ -136,10 +115,6 @@ def CudaTkWarpTile(r, c, layout="row"):
 
     @window_indexer(_CudaTkWarpTileIndexer)
     class Tile(base):
-        @classmethod
-        def global_(cls):
-            return _cuda_tk_scale_d_global
-
         @classmethod
         def alloc(cls, new_name, prim_type, shape, srcinfo):
             scalar_info = ScalarInfo(prim_type)
@@ -152,7 +127,7 @@ def CudaTkWarpTile(r, c, layout="row"):
             array_dims = "".join(f"[{n}]" for n in shape[:-2])
             # fmt: off
             assert _layout != "all", "Cannot allocate tile with 'all' layout"
-            return f"exo_CudaTkScaleD<::kittens::rt_{suffix}<{r}, {c}, ::kittens::ducks::rt_layout::{_layout}> > {new_name}{array_dims};"
+            return f"::kittens::rt_{suffix}<{r}, {c}, ::kittens::ducks::rt_layout::{_layout}> {new_name}{array_dims};"
 
         @classmethod
         def free(cls, new_name, prim_type, shape, srcinfo):
diff --git a/src/exo/spork/timelines.py b/src/exo/spork/timelines.py
index 29837c05..748bfb21 100644
--- a/src/exo/spork/timelines.py
+++ b/src/exo/spork/timelines.py
@@ -64,9 +64,6 @@ tma_to_gmem_async_instr = Instr_tl("tma_to_gmem_async_instr")
 """wgmma.mma_async instructions"""
 wgmma_async_instr = Instr_tl("wgmma_async_instr")
 
-"""Sets scale-d = 0 for the next wgmma.mma_async instr"""
-wgmma_zero_instr = Instr_tl("wgmma_zero_instr")
-
 """tcgen05.mma instructions"""
 tcgen05_mma_instr = Instr_tl("tcgen05_mma_instr")
 
@@ -94,7 +91,7 @@ cuda_async_instr_tl = [
     tcgen05_st_instr,
 ]
 
-cuda_basic_instr_tl = [cuda_in_order_instr, wgmma_zero_instr] + cuda_async_instr_tl
+cuda_basic_instr_tl = [cuda_in_order_instr] + cuda_async_instr_tl
 
 
 class DeviceScope(object):
@@ -215,7 +212,6 @@ tma_to_gmem_async_qual = Qual_tl("tma_to_gmem_async_qual", False)
 wgmma_async_rmem_a_qual = Qual_tl("wgmma_async_rmem_a_qual", True)
 wgmma_async_rmem_d_qual = Qual_tl("wgmma_async_rmem_d_qual", True)
 wgmma_async_smem_qual = Qual_tl("wgmma_async_smem_qual", False)
-wgmma_zero_qual = Qual_tl("wgmma_zero_qual", True)
 cuda_async_proxy_retired_qual = Qual_tl("cuda_async_proxy_retired_qual", False)
 tcgen05_smem_qual = Qual_tl("tcgen05_smem_qual", False)
 tcgen05_mma_tmem_qual = Qual_tl("tcgen05_mma_tmem_qual", True)
@@ -230,7 +226,6 @@ cuda_rmem_qual_tl_dict = {
     Sm80_cp_async_instr: cuda_in_order_rmem_qual,
     tma_to_smem_async_instr: cuda_in_order_rmem_qual,
     tma_to_gmem_async_instr: cuda_in_order_rmem_qual,
-    wgmma_zero_instr: [wgmma_zero_qual, wgmma_async_rmem_d_qual],
     # wgmma a/d has to be handled specially.
     #
     # cuda_in_order_rmem_qual is in the extended timeline set for tcgen05 access
@@ -305,7 +300,6 @@ _tcgen05_commit_quals = [
     tcgen05_shift_qual,
 ]
 
-# Intentionally excludes wgmma_zero_qual
 _cuda_device_quals = (
     _cuda_in_order_quals
     + [cpu_cuda_stream_qual, cuda_async_proxy_retired_qual]
@@ -321,7 +315,6 @@ _cuda_temporal_quals = [
     cuda_in_order_ram_qual,
     cuda_mbarrier_qual,
     cuda_async_proxy_retired_qual,
-    wgmma_zero_qual,
 ]
 
 _wgmma_rmem_quals = [
@@ -563,7 +556,6 @@ def generate_latex_table(out_file):
         (wgmma_async_rmem_a_qual, "wgA", r"$A$ parameter in registers accessed by \lighttt{wgmma.mma\_async}"),
         (wgmma_async_rmem_d_qual, "wgD", r"$D$ parameter in registers accessed by \lighttt{wgmma.mma\_async}"),
         (wgmma_async_smem_qual, "wgS", r"$A$ or $B$ parameter in SMEM accessed by \lighttt{wgmma.mma\_async}"),
-        (wgmma_zero_qual, "wg0", r"Special case for modeling \textsf{scale-d = 0}"),
         (cuda_async_proxy_retired_qual, "async", r"retired memory access made visible to the async proxy"),
     ]
     # fmt: on
