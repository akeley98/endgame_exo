diff --git a/src/exo/platforms/Sm90/Sm90_tk_mma.py b/src/exo/platforms/Sm90/Sm90_tk_mma.py
index d6d14ca4..71ceac9c 100644
--- a/src/exo/platforms/Sm90/Sm90_tk_mma.py
+++ b/src/exo/platforms/Sm90/Sm90_tk_mma.py
@@ -53,14 +53,26 @@ __all__ = [
     'Sm90_tk_mma_col_col',
     'Sm90_tk_mma_row_row',
     'Sm90_tk_mma_rmem_row',
-    'Sm90_tk_mma_col_row'
+    'Sm90_tk_mma_col_row',
+    'Sm90_tk_mma_row_col_zi',
+    'Sm90_tk_mma_rmem_col_zi',
+    'Sm90_tk_mma_col_col_zi',
+    'Sm90_tk_mma_row_row_zi',
+    'Sm90_tk_mma_rmem_row_zi',
+    'Sm90_tk_mma_col_row_zi',
+    'Sm90_tk_mma_row_col_acc',
+    'Sm90_tk_mma_rmem_col_acc',
+    'Sm90_tk_mma_col_col_acc',
+    'Sm90_tk_mma_row_row_acc',
+    'Sm90_tk_mma_rmem_row_acc',
+    'Sm90_tk_mma_col_row_acc'
 ]
 
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 1/6
+# wgmma.mma_async instr variant 1/18
 @instr
-class Sm90_tk_mma_row_col(make_basic_mma('row', 'col')):
+class Sm90_tk_mma_row_col(make_basic_mma('row', 'col', 'tile')):
     """"m(64) x n(N) x k(K) matrix multiply-accumulate
 
     A in row-major SMEM, B in column-major SMEM"""
@@ -88,9 +100,9 @@ class Sm90_tk_mma_row_col(make_basic_mma('row', 'col')):
                         )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 2/6
+# wgmma.mma_async instr variant 2/18
 @instr
-class Sm90_tk_mma_rmem_col(make_basic_mma('rmem', 'col')):
+class Sm90_tk_mma_rmem_col(make_basic_mma('rmem', 'col', 'tile')):
     """"m(64) x n(N) x k(K) matrix multiply-accumulate
 
     A in register tiles, B in column-major SMEM"""
@@ -118,9 +130,9 @@ class Sm90_tk_mma_rmem_col(make_basic_mma('rmem', 'col')):
                         )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 3/6
+# wgmma.mma_async instr variant 3/18
 @instr
-class Sm90_tk_mma_col_col(make_basic_mma('col', 'col')):
+class Sm90_tk_mma_col_col(make_basic_mma('col', 'col', 'tile')):
     """"m(64) x n(N) x k(K) matrix multiply-accumulate
 
     A in column-major SMEM (trans-a), B in column-major SMEM"""
@@ -148,9 +160,9 @@ class Sm90_tk_mma_col_col(make_basic_mma('col', 'col')):
                         )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 4/6
+# wgmma.mma_async instr variant 4/18
 @instr
-class Sm90_tk_mma_row_row(make_basic_mma('row', 'row')):
+class Sm90_tk_mma_row_row(make_basic_mma('row', 'row', 'tile')):
     """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
 
     A in row-major SMEM, B in row-major SMEM (trans-b)"""
@@ -179,9 +191,9 @@ class Sm90_tk_mma_row_row(make_basic_mma('row', 'row')):
                             )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 5/6
+# wgmma.mma_async instr variant 5/18
 @instr
-class Sm90_tk_mma_rmem_row(make_basic_mma('rmem', 'row')):
+class Sm90_tk_mma_rmem_row(make_basic_mma('rmem', 'row', 'tile')):
     """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
 
     A in register tiles, B in row-major SMEM (trans-b)"""
@@ -210,9 +222,393 @@ class Sm90_tk_mma_rmem_row(make_basic_mma('rmem', 'row')):
                             )
 
 #### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
-# wgmma.mma_async instr variant 6/6
+# wgmma.mma_async instr variant 6/18
 @instr
-class Sm90_tk_mma_col_row(make_basic_mma('col', 'row')):
+class Sm90_tk_mma_col_row(make_basic_mma('col', 'row', 'tile')):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
+
+    A in column-major SMEM (trans-a), B in row-major SMEM (trans-b)"""
+    def behavior(
+        N64: size,  # N / 64
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, 64 * N64],
+        # A @ Sm90_SmemSwizzled(128), indexed as [k, m]
+        A: [R][K, 64] @ Sm90_SmemSwizzled(128),
+        # B @ Sm90_SmemSwizzled(128), indexed as [n // 64, k, n % 64]
+        B: [R][N64, K, 64] @ Sm90_SmemSwizzled(128),
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for no in seq(0, N64):
+                    for ni in seq(0, 64):
+                        for k in seq(0, K):
+                            D[m_warp, mi, no * 64 + ni] += (
+                                A[k, m_warp * 16 + mi]
+                              * B[no, k, ni]
+                            )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 7/18
+@instr
+class Sm90_tk_mma_row_col_zi(make_basic_mma('row', 'col', 'arg')):
+    """"m(64) x n(N) x k(K) matrix multiply-accumulate
+
+    A in row-major SMEM, B in column-major SMEM"""
+    def behavior(
+        N: size,
+        K: size,
+        zero_init: bool,  # scale-d = !zero_init
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, N],
+        # A @ Sm90_SmemSwizzled(swizzle), indexed as [m, k]
+        A: [R][64, K],
+        # B @ Sm90_SmemSwizzled(swizzle), indexed as [n, k]
+        B: [R][N, K],
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for n in seq(0, N):
+                    if zero_init:
+                        D[m_warp, mi, n] = 0.0
+                    for k in seq(0, K):
+                        D[m_warp, mi, n] += (
+                            A[m_warp * 16 + mi, k]
+                          * B[n, k]
+                        )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 8/18
+@instr
+class Sm90_tk_mma_rmem_col_zi(make_basic_mma('rmem', 'col', 'arg')):
+    """"m(64) x n(N) x k(K) matrix multiply-accumulate
+
+    A in register tiles, B in column-major SMEM"""
+    def behavior(
+        N: size,
+        K: size,
+        zero_init: bool,  # scale-d = !zero_init
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, N],
+        # A @ Sm90_TkRmemTileA(K), split on M dim into 4 warps
+        A: [R][4, 16, K],
+        # B @ Sm90_SmemSwizzled(swizzle), indexed as [n, k]
+        B: [R][N, K],
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for n in seq(0, N):
+                    if zero_init:
+                        D[m_warp, mi, n] = 0.0
+                    for k in seq(0, K):
+                        D[m_warp, mi, n] += (
+                            A[m_warp, mi, k]
+                          * B[n, k]
+                        )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 9/18
+@instr
+class Sm90_tk_mma_col_col_zi(make_basic_mma('col', 'col', 'arg')):
+    """"m(64) x n(N) x k(K) matrix multiply-accumulate
+
+    A in column-major SMEM (trans-a), B in column-major SMEM"""
+    def behavior(
+        N: size,
+        K: size,
+        zero_init: bool,  # scale-d = !zero_init
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, N],
+        # A @ Sm90_SmemSwizzled(128), indexed as [k, m]
+        A: [R][K, 64] @ Sm90_SmemSwizzled(128),
+        # B @ Sm90_SmemSwizzled(swizzle), indexed as [n, k]
+        B: [R][N, K],
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for n in seq(0, N):
+                    if zero_init:
+                        D[m_warp, mi, n] = 0.0
+                    for k in seq(0, K):
+                        D[m_warp, mi, n] += (
+                            A[k, m_warp * 16 + mi]
+                          * B[n, k]
+                        )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 10/18
+@instr
+class Sm90_tk_mma_row_row_zi(make_basic_mma('row', 'row', 'arg')):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
+
+    A in row-major SMEM, B in row-major SMEM (trans-b)"""
+    def behavior(
+        N64: size,  # N / 64
+        K: size,
+        zero_init: bool,  # scale-d = !zero_init
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, 64 * N64],
+        # A @ Sm90_SmemSwizzled(swizzle), indexed as [m, k]
+        A: [R][64, K],
+        # B @ Sm90_SmemSwizzled(128), indexed as [n // 64, k, n % 64]
+        B: [R][N64, K, 64] @ Sm90_SmemSwizzled(128),
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for no in seq(0, N64):
+                    for ni in seq(0, 64):
+                        if zero_init:
+                            D[m_warp, mi, no * 64 + ni] = 0.0
+                        for k in seq(0, K):
+                            D[m_warp, mi, no * 64 + ni] += (
+                                A[m_warp * 16 + mi, k]
+                              * B[no, k, ni]
+                            )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 11/18
+@instr
+class Sm90_tk_mma_rmem_row_zi(make_basic_mma('rmem', 'row', 'arg')):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
+
+    A in register tiles, B in row-major SMEM (trans-b)"""
+    def behavior(
+        N64: size,  # N / 64
+        K: size,
+        zero_init: bool,  # scale-d = !zero_init
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, 64 * N64],
+        # A @ Sm90_TkRmemTileA(K), split on M dim into 4 warps
+        A: [R][4, 16, K],
+        # B @ Sm90_SmemSwizzled(128), indexed as [n // 64, k, n % 64]
+        B: [R][N64, K, 64] @ Sm90_SmemSwizzled(128),
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for no in seq(0, N64):
+                    for ni in seq(0, 64):
+                        if zero_init:
+                            D[m_warp, mi, no * 64 + ni] = 0.0
+                        for k in seq(0, K):
+                            D[m_warp, mi, no * 64 + ni] += (
+                                A[m_warp, mi, k]
+                              * B[no, k, ni]
+                            )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 12/18
+@instr
+class Sm90_tk_mma_col_row_zi(make_basic_mma('col', 'row', 'arg')):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
+
+    A in column-major SMEM (trans-a), B in row-major SMEM (trans-b)"""
+    def behavior(
+        N64: size,  # N / 64
+        K: size,
+        zero_init: bool,  # scale-d = !zero_init
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, 64 * N64],
+        # A @ Sm90_SmemSwizzled(128), indexed as [k, m]
+        A: [R][K, 64] @ Sm90_SmemSwizzled(128),
+        # B @ Sm90_SmemSwizzled(128), indexed as [n // 64, k, n % 64]
+        B: [R][N64, K, 64] @ Sm90_SmemSwizzled(128),
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for no in seq(0, N64):
+                    for ni in seq(0, 64):
+                        if zero_init:
+                            D[m_warp, mi, no * 64 + ni] = 0.0
+                        for k in seq(0, K):
+                            D[m_warp, mi, no * 64 + ni] += (
+                                A[k, m_warp * 16 + mi]
+                              * B[no, k, ni]
+                            )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 13/18
+@instr
+class Sm90_tk_mma_row_col_acc(make_basic_mma('row', 'col', 'one')):
+    """"m(64) x n(N) x k(K) matrix multiply-accumulate
+
+    A in row-major SMEM, B in column-major SMEM"""
+    def behavior(
+        N: size,
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, N],
+        # A @ Sm90_SmemSwizzled(swizzle), indexed as [m, k]
+        A: [R][64, K],
+        # B @ Sm90_SmemSwizzled(swizzle), indexed as [n, k]
+        B: [R][N, K],
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for n in seq(0, N):
+                    for k in seq(0, K):
+                        D[m_warp, mi, n] += (
+                            A[m_warp * 16 + mi, k]
+                          * B[n, k]
+                        )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 14/18
+@instr
+class Sm90_tk_mma_rmem_col_acc(make_basic_mma('rmem', 'col', 'one')):
+    """"m(64) x n(N) x k(K) matrix multiply-accumulate
+
+    A in register tiles, B in column-major SMEM"""
+    def behavior(
+        N: size,
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, N],
+        # A @ Sm90_TkRmemTileA(K), split on M dim into 4 warps
+        A: [R][4, 16, K],
+        # B @ Sm90_SmemSwizzled(swizzle), indexed as [n, k]
+        B: [R][N, K],
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for n in seq(0, N):
+                    for k in seq(0, K):
+                        D[m_warp, mi, n] += (
+                            A[m_warp, mi, k]
+                          * B[n, k]
+                        )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 15/18
+@instr
+class Sm90_tk_mma_col_col_acc(make_basic_mma('col', 'col', 'one')):
+    """"m(64) x n(N) x k(K) matrix multiply-accumulate
+
+    A in column-major SMEM (trans-a), B in column-major SMEM"""
+    def behavior(
+        N: size,
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, N],
+        # A @ Sm90_SmemSwizzled(128), indexed as [k, m]
+        A: [R][K, 64] @ Sm90_SmemSwizzled(128),
+        # B @ Sm90_SmemSwizzled(swizzle), indexed as [n, k]
+        B: [R][N, K],
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for n in seq(0, N):
+                    for k in seq(0, K):
+                        D[m_warp, mi, n] += (
+                            A[k, m_warp * 16 + mi]
+                          * B[n, k]
+                        )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 16/18
+@instr
+class Sm90_tk_mma_row_row_acc(make_basic_mma('row', 'row', 'one')):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
+
+    A in row-major SMEM, B in row-major SMEM (trans-b)"""
+    def behavior(
+        N64: size,  # N / 64
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, 64 * N64],
+        # A @ Sm90_SmemSwizzled(swizzle), indexed as [m, k]
+        A: [R][64, K],
+        # B @ Sm90_SmemSwizzled(128), indexed as [n // 64, k, n % 64]
+        B: [R][N64, K, 64] @ Sm90_SmemSwizzled(128),
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for no in seq(0, N64):
+                    for ni in seq(0, 64):
+                        for k in seq(0, K):
+                            D[m_warp, mi, no * 64 + ni] += (
+                                A[m_warp * 16 + mi, k]
+                              * B[no, k, ni]
+                            )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 17/18
+@instr
+class Sm90_tk_mma_rmem_row_acc(make_basic_mma('rmem', 'row', 'one')):
+    """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
+
+    A in register tiles, B in row-major SMEM (trans-b)"""
+    def behavior(
+        N64: size,  # N / 64
+        K: size,
+        # D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps
+        D: [R][4, 16, 64 * N64],
+        # A @ Sm90_TkRmemTileA(K), split on M dim into 4 warps
+        A: [R][4, 16, K],
+        # B @ Sm90_SmemSwizzled(128), indexed as [n // 64, k, n % 64]
+        B: [R][N64, K, 64] @ Sm90_SmemSwizzled(128),
+    ):
+        # Note, SMEM stride requirements enforced in codegen.
+        # Right-most ("leading") dimension must be densely packed.
+        # The other ("strided") dimension must be strided by `swizzle` BYTES,
+        # i.e. swizzle / sizeof(Element) elements.
+        for m_warp in seq(0, 4):
+            for mi in seq(0, 16):
+                for no in seq(0, N64):
+                    for ni in seq(0, 64):
+                        for k in seq(0, K):
+                            D[m_warp, mi, no * 64 + ni] += (
+                                A[m_warp, mi, k]
+                              * B[no, k, ni]
+                            )
+
+#### Autogenerated by gen_Sm90_tk_mma.py; do not hand-edit! ####
+# wgmma.mma_async instr variant 18/18
+@instr
+class Sm90_tk_mma_col_row_acc(make_basic_mma('col', 'row', 'one')):
     """"m(64) x n(64 * N64) x k(K) matrix multiply-accumulate
 
     A in column-major SMEM (trans-a), B in row-major SMEM (trans-b)"""
diff --git a/src/exo/platforms/Sm90/Sm90_tk_mma_impl.py b/src/exo/platforms/Sm90/Sm90_tk_mma_impl.py
index 0f8ea6e7..f1010323 100644
--- a/src/exo/platforms/Sm90/Sm90_tk_mma_impl.py
+++ b/src/exo/platforms/Sm90/Sm90_tk_mma_impl.py
@@ -33,7 +33,10 @@ def Sm90_TkRmemTileD(N: int):
     return Tile
 
 
-def make_basic_mma(a_mode, b_mode):
+def make_basic_mma(a_mode, b_mode, scale_d_mode="tile"):
+    # scale_d_mode: "tile" (legacy magic exo_CudaTkScaleD member),
+    # "arg" (zero_init: bool behavior arg), "one" (hardwired scale-d = 1)
+    assert scale_d_mode in ("tile", "arg", "one")
     assert a_mode in ("row", "col", "rmem")
     assert b_mode in ("row", "col")
 
@@ -175,7 +178,12 @@ def make_basic_mma(a_mode, b_mode):
                 if K_iters != 1:
                     lines.append(f"  // K = {k * K_native} out of {self.K}")
                 d_arg = args.D.index()
-                scale_d_arg = args.D.index(get_scale_d=True)
+                if scale_d_mode == "tile":
+                    scale_d_arg = args.D.index(get_scale_d=True)
+                elif scale_d_mode == "one" or k != 0:
+                    scale_d_arg = "1"
+                else:
+                    scale_d_arg = f"int(!({args.zero_init.index()}))"
                 ptx = InlinePtxGen(instr_fmt, volatile=True)
 
                 # p=scale_d arg to wgmma
@@ -231,7 +239,8 @@ def make_basic_mma(a_mode, b_mode):
                 # Write out the code for this K iteration.
                 # Implicitly reset scale_d flag after wgmma.
                 lines.extend(ptx.as_c_lines(tab="  "))
-                lines.append(f"  {scale_d_arg} = 1;")
+                if scale_d_mode == "tile":
+                    lines.append(f"  {scale_d_arg} = 1;")
                 lines.append("}")
             # End for k in range(K_iters)
             return lines
diff --git a/src/exo/platforms/Sm90/gen_Sm90_tk_mma.py b/src/exo/platforms/Sm90/gen_Sm90_tk_mma.py
index de92dd80..9f6d88e0 100644
--- a/src/exo/platforms/Sm90/gen_Sm90_tk_mma.py
+++ b/src/exo/platforms/Sm90/gen_Sm90_tk_mma.py
@@ -67,12 +67,13 @@ class Sm90_tk_zero_scale_d(InstrInfo):
         self.access_info["D"].distributed_coll_units = (cuda_warp, )
 
     def codegen(self, args):
-        return [f"{args.D.index()}.scale_d = 0;"]
+        return [f"{args.D.index(get_scale_d=True)} = 0;"]
 ''')
 
 
-def append_instr(a_mode: str, b_mode: str):
-    instr_name = f"Sm90_tk_mma_{a_mode}_{b_mode}"
+def append_instr(a_mode: str, b_mode: str, scale_d_mode: str = "tile"):
+    suffix = {"tile": "", "arg": "_zi", "one": "_acc"}[scale_d_mode]
+    instr_name = f"Sm90_tk_mma_{a_mode}_{b_mode}{suffix}"
 
     M = "64"
     if a_mode == "rmem":
@@ -90,7 +91,7 @@ def append_instr(a_mode: str, b_mode: str):
 
     lines = []
     lines.append("@instr")
-    lines.append(f"class {instr_name}(make_basic_mma({a_mode!r}, {b_mode!r})):")
+    lines.append(f"class {instr_name}(make_basic_mma({a_mode!r}, {b_mode!r}, {scale_d_mode!r})):")
     lines.append(f'    """"m({M}) x n({N}) x k(K) matrix multiply-accumulate')
     lines.append("")
     lines.append(f'    A in {A_comment}, B in {B_comment}"""')
@@ -104,6 +105,8 @@ def append_instr(a_mode: str, b_mode: str):
     else:
         lines.append(indent + "N: size,")
     lines.append(indent + "K: size,")
+    if scale_d_mode == "arg":
+        lines.append(indent + "zero_init: bool,  # scale-d = !zero_init")
 
     # D parameter
     lines.append(indent + "# D @ Sm90_TkRmemTileD(N), split on M dim into 4 warps")
@@ -154,6 +157,9 @@ def append_instr(a_mode: str, b_mode: str):
         lines.append(indent + "for n in seq(0, N):")
         indent += "    "
         n = "n"
+    if scale_d_mode == "arg":
+        lines.append(indent + "if zero_init:")
+        lines.append(indent + f"    D[m_warp, mi, {n}] = 0.0")
     lines.append(indent + "for k in seq(0, K):")
     indent += "    "
 
@@ -177,9 +183,10 @@ def append_instr(a_mode: str, b_mode: str):
     _instr_defs.append("\n".join(lines))
 
 
-for b_mode in ("col", "row"):
-    for a_mode in ("row", "rmem", "col"):
-        append_instr(a_mode, b_mode)
+for scale_d_mode in ("tile", "arg", "one"):
+    for b_mode in ("col", "row"):
+        for a_mode in ("row", "rmem", "col"):
+            append_instr(a_mode, b_mode, scale_d_mode)
 
 
 write_chunk(
diff --git a/src/exo/platforms/Sm90/tk_gemm_util.py b/src/exo/platforms/Sm90/tk_gemm_util.py
index f2c590fb..35899551 100644
--- a/src/exo/platforms/Sm90/tk_gemm_util.py
+++ b/src/exo/platforms/Sm90/tk_gemm_util.py
@@ -1391,6 +1391,11 @@ def find_parent_loop(p: Procedure, cursor, iter_name):
     return cursor
 
 
+import os
+WGMMA_ZERO_RESEARCH = os.environ.get("WGMMA_ZERO_RESEARCH", "0") != "0"
+WGMMA_ZERO_RESEARCH_PRINT = os.environ.get("WGMMA_ZERO_RESEARCH_PRINT", "0") != "0"
+
+
 def schedule_gemm(config: GemmConfig, cases=None):
     # fmt: off
     assert config.A_major == "row", "not supported"
@@ -1579,6 +1584,19 @@ def schedule_gemm(config: GemmConfig, cases=None):
     gemm = divide_loop(gemm, k_loop, smem_K, ("iter_k", "sub_iter_k"), tail="guard")
     iter_k_loop = gemm.forward(k_loop)
     sub_iter_k_loop = iter_k_loop.only_child()
+    if WGMMA_ZERO_RESEARCH:
+        # D_rmem = 0; for iter_k: ...
+        # -> for iter_k: (if iter_k == 0: D_rmem = 0); ...
+        D_zero = gemm.forward(D_zero)
+        gemm = add_loop(gemm, D_zero, "iter_k", f"(K_cluster + {smem_K - 1}) / {smem_K}", guard=True)
+        iter_k_loop = gemm.forward(iter_k_loop)
+        zero_iter_k_loop = iter_k_loop.prev()
+        gemm = fuse(gemm, zero_iter_k_loop, iter_k_loop)
+        iter_k_loop = gemm.forward(zero_iter_k_loop)  # fuse deletes 2nd loop
+        gap_after_main = iter_k_loop.after()
+        D_zero = gemm.forward(D_zero)
+        if WGMMA_ZERO_RESEARCH_PRINT:
+            print(gemm)
     k_lifts = 0
     parent = iter_k_loop.parent()
     while True:
@@ -1588,11 +1606,13 @@ def schedule_gemm(config: GemmConfig, cases=None):
         k_lifts += 1
         parent = parent.parent()
     gemm = lift_alloc(gemm, D_rmem, n_lifts=k_lifts)
-    gemm = fission(gemm, gap_before_main, n_lifts=k_lifts, unsafe_disable_checks=unsafe)
+    if not WGMMA_ZERO_RESEARCH:
+        gemm = fission(gemm, gap_before_main, n_lifts=k_lifts, unsafe_disable_checks=unsafe)
     gemm = fission(gemm, gap_after_main, n_lifts=k_lifts, unsafe_disable_checks=unsafe)
     for i in range(k_lifts):
         gemm = lift_scope(gemm, iter_k_loop)
-    gap_before_main = gemm.forward(gap_before_main)
+    if not WGMMA_ZERO_RESEARCH:
+        gap_before_main = gemm.forward(gap_before_main)
     gap_after_main = gemm.forward(gap_after_main)
     D_rmem = gemm.forward(D_rmem)
     iter_k_loop = gemm.forward(iter_k_loop)
@@ -1726,7 +1746,9 @@ def schedule_gemm(config: GemmConfig, cases=None):
     wgmma_mw_cursor = wgmma_ms_cursor.only_child(1)
     assert wgmma_mw_cursor.name() == "mw"
     gemm = unsafe_remove_if(gemm, wgmma_ms_cursor, True)
-    gemm = replace(gemm, wgmma_mw_cursor, Sm90_tk_mma_row_col)
+    if WGMMA_ZERO_RESEARCH_PRINT:
+        print(gemm.forward(wgmma_ms_cursor))
+    gemm = replace(gemm, wgmma_mw_cursor, Sm90_tk_mma_row_col_zi if WGMMA_ZERO_RESEARCH else Sm90_tk_mma_row_col)
     gemm = insert_fence(gemm, wgmma_ms_cursor.before(), wgmma_fence_1, wgmma_fence_2)
     # Arrive(wgmma_async, 1) >> wgmma_cg[cta_m, cta_n, wg_m]
     gemm = insert_arrive(gemm, wgmma_ms_cursor.after(), wgmma_async, "wgmma_cg[cta_m, cta_n, wg_m]")
@@ -1741,12 +1763,13 @@ def schedule_gemm(config: GemmConfig, cases=None):
 
     # Finalize zero prologue.
     # We have to unsafely remove the if-guards to substitute the zero instr.
-    D_zero = gemm.forward(D_zero)
-    zero_m_loop = find_parent_loop(gemm, D_zero, "wg_m")
-    gemm = unsafe_remove_if(gemm, zero_m_loop, True)
-    gemm = wrap_with_context(gemm, zero_m_loop, CudaWarps(name="consumer"))
-    zero_m_loop = gemm.forward(zero_m_loop)
-    gemm = replace(gemm, zero_m_loop.find("for mw in _:_"), Sm90_tk_zero_scale_d)
+    if not WGMMA_ZERO_RESEARCH:
+        D_zero = gemm.forward(D_zero)
+        zero_m_loop = find_parent_loop(gemm, D_zero, "wg_m")
+        gemm = unsafe_remove_if(gemm, zero_m_loop, True)
+        gemm = wrap_with_context(gemm, zero_m_loop, CudaWarps(name="consumer"))
+        zero_m_loop = gemm.forward(zero_m_loop)
+        gemm = replace(gemm, zero_m_loop.find("for mw in _:_"), Sm90_tk_zero_scale_d)
 
     # Finalize write-to-C epilogue.
     # Need to wait for wgmma beforehand, and do a final arrive to war barrier.
